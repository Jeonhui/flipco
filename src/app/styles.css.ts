import { style } from "@vanilla-extract/css"

export const flipDateClock = style({
  position: "absolute",
  top: "6rem"
})

export const pipPlayerContainer = style({
  position: "relative",
  top: 0,
  left: 0,
  width: "100%",
  flex: 1,
  height: "100%"
})

export const controlContainer = style({

})